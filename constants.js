export const RETRIES = 3;
export const DELAY = 1000; // 1 second
export const CONCURRENCY_LIMIT = 50 // Limit the number of concurrent fetches
export const REQUEST_TIMEOUT = 180000; // 3 minutes in milliseconds
